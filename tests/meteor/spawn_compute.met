def compute(id: int, n: int)
    result = 0
    i = 0
    while i < n
        result = result + i
        i = i + 1
    print(id)
    print(result)

spawn compute(1, 100)
spawn compute(2, 50)
print(0)
